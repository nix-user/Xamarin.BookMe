<?xml version="1.0" encoding="utf-8" ?>
<pages:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:BookMeMobile.Pages;assembly=BookMeMobile"
             xmlns:render="clr-namespace:BookMeMobile.Render;assembly=BookMeMobile"
             x:Class="BookMeMobile.Pages.ProfilePage"
             Title="Profile">
  <AbsoluteLayout x:Name="rootLayout" Padding="0" VerticalOptions="CenterAndExpand">
    <StackLayout  Orientation="Horizontal" AbsoluteLayout.LayoutBounds="0.5, 0.42, -1, -1" AbsoluteLayout.LayoutFlags="PositionProportional">
      <render:Loader x:Name="loader" VerticalOptions="Center" />
    </StackLayout>
    <StackLayout BindingContext="{Binding}" VerticalOptions="CenterAndExpand" Padding="10,0,40,0">
      <TableView>
        <TableRoot>
          <TableSection>
            <EntryCell x:Name="txtFavoriteRoomCell" Keyboard="Default" Label="Любимая комната:" Text="{Binding FavoriteRoom}"/>
          </TableSection>
          <TableSection>
            <EntryCell x:Name="txtMyRoomCell" Keyboard="Numeric" Label="Мой этаж:" Text="{Binding MyFloor}"/>
          </TableSection>
        </TableRoot>
      </TableView>
        <Button x:Name="btnSave" Text="Сохранить" Command="{Binding ChangeSaveCommand}" IsEnabled="{Binding IsEnableButtonSave}"/>
    </StackLayout>
  </AbsoluteLayout>
</pages:BasePage>